{% extends 'AdminBundle::layout.html.twig' %}

{% block title %}Ebook{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/admin/plugins/data-tables/DT_bootstrap.css') }}"/>
{% endblock %}

{% block content %}
    <div class="portlet box yellow">
        <div class="portlet-title">
            <div class="caption"><i class="icon-list"></i>Mostrar Ebook</div>
            <div class="tools">
                <a href="javascript:;" class="collapse"></a>
                <a href="#portlet-config" data-toggle="modal" class="config"></a>
                <a href="javascript:;" class="reload"></a>
                <a href="javascript:;" class="remove"></a>
            </div>
        </div>
        <div class="portlet-body">
            <div class="clearfix">
                <div class="btn-group">
                    <a href="{{ path('management_audio_ebook') }}" class="btn green">
                        Volver al listado
                    </a>
                </div>
                <div class="btn-group pull-right">
                    {{ form(delete_form) }}
                </div>
            </div>

            <table class="table table-striped table-hover">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ entity.name ~ '.' ~ entity.path}}</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        {% for flashMessage in app.session.flashbag.get('notice') %}
        Codes.showMessage('Información', '{{ flashMessage  }}');
        {% endfor %}
    </script>
{% endblock %}
