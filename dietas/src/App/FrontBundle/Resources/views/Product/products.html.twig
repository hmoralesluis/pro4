{% extends 'FrontBundle::layout.html.twig' %}

{% block title %}Our {{ name }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{{ name }}
                    <small>Special offert, just choice an option to start changing your life</small>
                </h1>
                <ol class="breadcrumb">
                    <li><a href="{{ path('homepage') }}">Home</a></li>
                    <li class="active">{{ name }}</li>
                </ol>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <a href="{{ path('product', {'id': 1 }) }}"><img src="{{ asset('bundles/front/img/600x300.png') }}" class="img-responsive"></a>
            </div>
            <div class="col-md-6">
                <h3><a href="{{ path('product', {'id': 1 }) }}">The Product description's name</a></h3>

                <p>by <a href="#">owner</a></p>

                <p>This is a very basic starter template for a product description. It makes use of Font Awesome icons that
                    are built into the 'Modern Business' template, and it makes use of the Bootstrap 3 pager at the
                    bottom of the page.</p>
                <a class="btn btn-primary" href="{{ path('product', {'id': 1 }) }}">Read More <i class="fa fa-angle-right"></i></a>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-6">
                <a href="{{ path('product', {'id': 2 }) }}"><img src="{{ asset('bundles/front/img/600x300.png') }}" class="img-responsive"></a>
            </div>
            <div class="col-md-6">
                <h3><a href="{{ path('product', {'id': 2 }) }}">The Product description's name</a></h3>

                <p>by <a href="#">owner</a></p>

                <p>This is a very basic starter template for a product description. It makes use of Font Awesome icons that
                    are built into the 'Modern Business' template, and it makes use of the Bootstrap 3 pager at the
                    bottom of the page.</p>
                <a class="btn btn-primary" href="{{ path('product', {'id': 2 }) }}">Read More <i class="fa fa-angle-right"></i></a>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-6">
                <a href="{{ path('product', {'id': 3 }) }}"><img src="{{ asset('bundles/front/img/600x300.png') }}" class="img-responsive"></a>
            </div>
            <div class="col-md-6">
                <h3><a href="{{ path('product', {'id': 3 }) }}">The Product description's name</a></h3>

                <p>by <a href="#">owner</a></p>

                <p>This is a very basic starter template for a product description. It makes use of Font Awesome icons that
                    are built into the 'Modern Business' template, and it makes use of the Bootstrap 3 pager at the
                    bottom of the page.</p>
                <a class="btn btn-primary" href="{{ path('product', {'id': 3 }) }}">Read More <i class="fa fa-angle-right"></i></a>
            </div>
        </div>
        <hr>
        <div class="row">
            <ul class="pager">
                <li class="previous"><a href="#">&larr; Older</a></li>
                <li class="next"><a href="#">Newer &rarr;</a></li>
            </ul>
        </div>
    </div>
{% endblock %}

